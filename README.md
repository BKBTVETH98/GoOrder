# GoOrder

GoOrder — пример сервиса на Go для обработки заказов, использующий Kafka и базу данных.  
Проект демонстрирует организацию кода, работу с моделью, инициализацию БД и обмен сообщениями через Kafka.

---

## 📁 Структура проекта

```
.
├── cmd/
│   └── main.go           # Точка входа
├── internal/
│   ├── db/
│   │   └── init/
│   │       └── init.sql  # Скрипт инициализации БД
│   └── model/
│       └── model.go      # Структуры данных
├── kafka/
│   ├── consumer.go       # Kafka-консьюмер
│   └── producer.go       # Kafka-продюсер
├── model/
│   └── model.json        # Пример JSON-модели
├── Dockerfile
├── docker-compose.yml
├── go.mod
└── README.md
```

---

## ⚙️ Требования

- Go 1.20+ (или актуальная версия)
- Docker & Docker Compose (для автоматического развёртывания)
- Kafka (в контейнере через `docker-compose`, либо внешний)
- PostgreSQL/MySQL/другая СУБД (через `init.sql`)

---

## 🚀 Быстрый старт

1. **Склонируйте репозиторий:**
   ```
   git clone <repo-url> goorder
   cd goorder
   ```

2. **Запустите зависимые сервисы:**
   ```
   docker-compose up -d
   ```
   В составе: Kafka, Zookeeper, база данных.

3. **Инициализируйте схему БД:**
   ```
   docker exec -it <db-container> sh -c "psql -U user -d orders -f /path/to/init.sql"
   ```
   (или используйте `init.sql` вручную на вашей СУБД)

4. **Соберите и запустите сервис:**
   ```
   go build -o goorder ./cmd
   ./goorder
   ```

   Или через `go run`:
   ```
   go run ./cmd
   ```

---

## 🧱 Основные компоненты

- `internal/model` — описания структур заказов и связанных сущностей.
- `kafka/producer.go` — отправляет сообщения в тему Kafka.
- `kafka/consumer.go` — читает сообщения и обрабатывает их.
- `internal/db/init/init.sql` — SQL для создания таблиц.
- `cmd/main.go` — конфигурация, запуск продюсера и консюмера.

---

## 🛠️ Настройка

Параметры (адреса Kafka, БД и т.п.) задаются через переменные окружения или конфиг в `main.go`. Дополнительные аргументы можно добавить по необходимости.

---

## 📦 Docker

**Сборка образа:**
```
docker build -t goorder .
```

**Запуск:**
```
docker run --env-file .env -p 8080:8080 goorder
```

`docker-compose.yml` уже содержит всё для локального запуска (Kafka + БД + сервис).

---

## 📌 Дополнительно

- Модель данных находится в `model/model.json` — пример структуры заказов.
- Можно расширять проект, добавляя REST API, другие брокеры, аутентификацию и т.п.

---